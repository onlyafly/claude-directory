# Validate Everything

Run this command to perform a comprehensive validation of all the specs:

    openspec validate --all --strict --json

Then fix any issues found, prioritizing CRITICAL issues first. After fixing, re-run the validation to ensure all issues are resolved.
